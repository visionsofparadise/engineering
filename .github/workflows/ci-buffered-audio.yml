name: Buffered Audio CI

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/ci-buffered-audio.yml"
      - "package.json"
      - "package-lock.json"
      - "turbo.json"
      - "tsconfig.json"
      - "packages/buffered-audio-nodes-core/**"
      - "packages/buffered-audio-nodes-utils/**"
      - "packages/buffered-audio-nodes/**"
  pull_request:
    paths:
      - ".github/workflows/ci-buffered-audio.yml"
      - "package.json"
      - "package-lock.json"
      - "turbo.json"
      - "tsconfig.json"
      - "packages/buffered-audio-nodes-core/**"
      - "packages/buffered-audio-nodes-utils/**"
      - "packages/buffered-audio-nodes/**"
  workflow_dispatch:

jobs:
  buffered-audio:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22.14"

      - name: Install dependencies
        run: npm ci

      - name: Check (topo-ordered across all three packages)
        run: npx turbo check --filter=@e9g/buffered-audio-nodes

      - name: Unit (topo-ordered)
        run: npx turbo unit --filter=@e9g/buffered-audio-nodes

      - name: Build (topo-ordered)
        run: npx turbo build --filter=@e9g/buffered-audio-nodes

      - name: Docs (drift check)
        run: |
          npx turbo docs --filter=@e9g/buffered-audio-nodes
          git diff --exit-code packages/buffered-audio-nodes/README.md
