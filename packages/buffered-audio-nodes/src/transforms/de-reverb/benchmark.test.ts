import { describe, it } from "vitest";

// Benchmark skeleton. Phases 8–9 will replace this placeholder with a real
// benchmark run against `audio.testVoice` similar to the de-reverb-wpe
// benchmark, once the node's `_process` orchestration is in place.
describe("deReverb benchmark", () => {
	it.skip("benchmarks deReverb (placeholder — Phase 8 fills this in)", () => {
		// Intentionally empty.
	});
});
